import { createMuiTheme }  from '@mui/material/styles';

const theme = createMuiTheme({
  palette: {
    primary: { main: '#0057b7' },
    warning: { main: '#ffd700' },
  },
});


export default theme;